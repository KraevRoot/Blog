%h1 Create new Post

= simple_form_for @post do |f|
  .form-inputs
    = f.input :title, placeholder: 'Enter title'
    = f.input :content, as: :text, placeholder: 'Content'
  = f.button :submit, 'Create post'
  
